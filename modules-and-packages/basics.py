# Reuse code across multiple files by importing
# A module is just a python file

# Built in modules
import random as so_random   #import with another name
import math
import time
import pandas as pd



print(random.randint(1, 100))
print(math.sqrt(58))

print(random.choice(['June', 'Nicole', 'Maria', 'Mercy']))
data = request.urlopen('http://example.com/')

