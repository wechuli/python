# In python we can also throw errors using the raise keyword. This is helpful when creating your own kinds of exception and error messages

# raise ValueError('invalid value')
raise NameError('name blah blah')

